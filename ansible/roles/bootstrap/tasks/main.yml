---
- name: apt update
  apt:
    update_cache: yes
  become: true

- name: required packages are latest
  apt:
    name:
    - ansible
    - python-pip
    state: latest
  become: true

- name: awscli is latest
  pip:
    name: awscli
    state: latest
  become: true
